# Known Limitations

Known constraints and limitations of the system and of the AI workflow itself.

- The agent runtime (OpenCode / Claude Code) must be available in CI.
- Context is intentionally limited; agents may need `/regenerate-context`.
- Architecture changes require explicit human approval via `/update-plan`.
